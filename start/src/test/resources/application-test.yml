spring:
  datasource:
    url: jdbc:tc:mysql:8.0:///midgard_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  flyway:
    enabled: true
    locations: classpath:db/migration
