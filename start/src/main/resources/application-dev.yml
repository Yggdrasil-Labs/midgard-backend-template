spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:midgard}
    username: ${DB_USER:root}
    password: ${DB_PASSWORD:root}

logging:
  level:
    io.yggdrasil.labs.midgard.*: debug
    root: info
